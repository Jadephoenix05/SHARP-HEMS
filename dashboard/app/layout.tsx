import type { Metadata } from 'next';
import { Inter, JetBrains_Mono } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-sans',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
});

export const metadata: Metadata = {
  title: 'SHARP — Smart Home Appliance Response Platform',
  description:
    'Cyber-Physical Home Energy Management System responding to grid peak events with guaranteed necessity load protection',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body className="bg-[#f5f9ff] text-[#172b4d] antialiased min-h-screen">
        {children}
      </body>
    </html>
  );
}